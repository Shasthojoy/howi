language: go
go:
  - master
# notifications
notifications:
  email: false

jobs:
  fast_finish: true
  allow_failures:
    - go: master
  include:
    - stage: test
      script: echo "2"
      env: JOB=test
    - stage: build
      script: echo "3"
      env: JOB=cmd howi-ce
    - stage: deploy
      script: echo "4"
      env: JOB=deploy howi-ce
