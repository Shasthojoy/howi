language: go
go:
  - master
# notifications
notifications:
  email: false
# Jobs
jobs:
  fast_finish: true
  allow_failures:
    - go: master
  include:
    # TEST
    - stage: test
      env: JOB=test
      script: echo "2"
    
    - env: JOB=code coverage
      script:
      - go get github.com/mattn/goveralls
      - goveralls -service=travis-ci
    # BUILD
    - stage: build
      env: JOB=cmd howi-ce
      script: echo "3"
    # DEPLOY
    - stage: deploy
      env: JOB=deploy howi-ce
      script: echo "4"
